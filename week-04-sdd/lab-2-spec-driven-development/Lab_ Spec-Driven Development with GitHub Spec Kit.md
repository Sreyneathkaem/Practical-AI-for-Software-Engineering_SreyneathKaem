# Lab: Spec-Driven Development with GitHub Spec Kit

**Course:** AI-Enabled Software Development / Software Engineering
**Lab Topic:** Spec-Driven Development (SDD)
**Tool:** GitHub Spec Kit
**Duration:** 3 Hours
**Mode:** Individual or Pair Work

---

# 1. Lab Overview

AI coding assistants can generate code very quickly. However, generating code quickly does not necessarily mean that we are building the **right software**.

A common workflow is:

> Idea → Prompt AI → Generate Code → Fix Problems → Prompt Again → More Code

In this lab, you will use **Spec-Driven Development (SDD)** instead:

> Idea → Specification → Clarification → Technical Plan → Tasks → Analysis → Implementation

Instead of treating documentation as something written after development, SDD treats the **specification as the source of truth for implementation**.

You will use **GitHub Spec Kit** with an AI coding agent to take one application feature through the complete SDD lifecycle.

---

# 2. Learning Objectives

By the end of this lab, you should be able to:

1. Explain the purpose of Spec-Driven Development.
2. Install and initialize GitHub Spec Kit.
3. Identify the main files and directories generated by Spec Kit.
4. Define project-wide engineering principles using `/speckit.constitution`.
5. Translate a feature idea into a structured specification using `/speckit.specify`.
6. Identify and resolve ambiguous requirements using `/speckit.clarify`.
7. Transform requirements into a technical implementation plan using `/speckit.plan`.
8. Generate implementation work using `/speckit.tasks`.
9. Check consistency between specification, plan, and tasks using `/speckit.analyze`.
10. Implement a feature using `/speckit.implement`.
11. Evaluate whether the final implementation satisfies the original specification.

---

# 3. The SDD Workflow

During this lab, you will follow this workflow:

```text
                Project Principles
                       │
                       ▼
              /speckit.constitution
                       │
                       ▼
                    IDEA
                       │
                       ▼
                /speckit.specify
                       │
                       ▼
                  specification
                       │
                       ▼
                /speckit.clarify
                       │
                       ▼
               clarified spec
                       │
                       ▼
                  /speckit.plan
                       │
                       ▼
             technical architecture
                       │
                       ▼
                 /speckit.tasks
                       │
                       ▼
              implementation tasks
                       │
                       ▼
                /speckit.analyze
                       │
                 inconsistencies?
                  /          \
                yes           no
                │              │
                └── revise ────┘
                               │
                               ▼
                     /speckit.implement
                               │
                               ▼
                       Working Feature
                               │
                               ▼
                     Verify Against Spec
```

The important idea is:

> **Do not jump directly from an idea to implementation.**

---

# 4. Before You Start

You need:

* Git
* A code editor
* `uv`
* GitHub Spec Kit
* An AI coding agent supported by your environment
* An existing application or starter project

Check Git:

```bash
git --version
```

Check `uv`:

```bash
uv --version
```

---

# Part 1 — Install Spec Kit

**Suggested time: 15 minutes**

## Step 1.1 — Install Specify CLI

Install Spec Kit:

```bash
uv tool install specify-cli
```

Verify the installation:

```bash
specify version
```

You should see the installed Specify CLI version.

---

## Step 1.2 — Explore the CLI

Run:

```bash
specify --help
```

Spend a few minutes exploring the available commands.

### Question

Write down two things that `specify` can do besides initializing a project.

1. ---

2. ---

---

# Part 2 — Initialize Spec Kit

**Suggested time: 15 minutes**

You can either create a new application or add Spec Kit to an existing project.

For this lab, we will assume that you already have a project.

Navigate to your project:

```bash
cd your-project
```

Initialize Spec Kit in the current project:

```bash
specify init --here
```

Select the AI coding agent you are using when prompted.

If your instructor specifies an integration, you may instead use:

```bash
specify init --here --integration <agent>
```

For example:

```bash
specify init --here --integration claude
```

or:

```bash
specify init --here --integration copilot
```

---

# Part 3 — Understand the Project Structure

**Suggested time: 15 minutes**

Do **not** start developing yet.

Inspect your project.

Depending on your selected integration, Spec Kit will install project templates, scripts, agent instructions, and other supporting files.

One important directory is:

```text
.specify/
```

You may see content such as:

```text
.specify/
├── memory/
├── scripts/
└── templates/
```

As you create features, Spec Kit will also maintain feature-specific artifacts under a structure similar to:

```text
specs/
└── 001-feature-name/
    ├── spec.md
    ├── plan.md
    ├── research.md
    ├── data-model.md
    ├── quickstart.md
    ├── contracts/
    └── tasks.md
```

Not every feature necessarily produces every supporting artifact.

### Exploration Task

Locate the files or folders related to:

| Artifact        | Purpose                                         |
| --------------- | ----------------------------------------------- |
| `.specify/`     | Spec Kit configuration and supporting resources |
| Constitution    | Project-wide engineering principles             |
| `spec.md`       | Functional requirements and user expectations   |
| `plan.md`       | Technical implementation approach               |
| `tasks.md`      | Actionable implementation work                  |
| `contracts/`    | Interfaces/API contracts when applicable        |
| `data-model.md` | Data structures when applicable                 |

### Checkpoint

Before continuing, explain to your partner:

> What is the difference between `spec.md`, `plan.md`, and `tasks.md`?

Write your answer:

**spec.md**

---

**plan.md**

---

**tasks.md**

---

---

# Part 4 — Establish the Project Constitution

**Suggested time: 20 minutes**

Before specifying individual features, establish the engineering principles that should govern the project.

Launch your AI coding agent inside the project.

Run:

```text
/speckit.constitution
```

Do not simply ask it to create generic principles.

Provide meaningful engineering expectations.

Example:

```text
/speckit.constitution

Create project principles based on:

- readable and maintainable code
- separation of concerns
- simple architecture over unnecessary complexity
- automated testing for important business logic
- input validation and error handling
- secure management of secrets
- consistent user experience
- documentation for important architectural decisions
- no implementation should be considered complete unless its acceptance criteria can be verified
```

Open the generated constitution.

### Inspect

Identify at least three rules that could affect future implementation.

| Principle | How might this affect implementation? |
| --------- | ------------------------------------- |
| 1.        |                                       |
| 2.        |                                       |
| 3.        |                                       |

### Think

Why should the constitution be created **before** the feature plan?

---

---

---

# Part 5 — Select a Feature

**Suggested time: 10 minutes**

Choose **one small but meaningful feature** from your existing project.

The feature must be possible to implement during the lab.

Good examples:

* Search notes
* Add items to favorites
* Filter tasks by status
* User profile editing
* Dark mode preference
* Export records to CSV
* Password visibility toggle
* Add comments to a post
* Search students by name
* Mark notifications as read

Avoid very large features such as:

* Build an entire authentication system
* Build an e-commerce application
* Build an entire social network
* Build a complete AI chatbot platform

Write your feature:

> **Feature:** _______________________________________________

---

# Part 6 — Specify: Define WHAT and WHY

**Suggested time: 25 minutes**

Now create the specification.

Run:

```text
/speckit.specify
```

Describe the feature from the **user's perspective**.

Focus on:

* Who needs the feature?
* What should they be able to do?
* Why do they need it?
* What behavior should they observe?
* What counts as success?

Avoid telling the AI exactly how to implement it.

### Example

Instead of:

```text
/speckit.specify

Create a React search component using useState,
call GET /api/tasks?q=query and debounce it by 300ms.
```

write:

```text
/speckit.specify

Users need to search their tasks by title.

A user can enter part or all of a task title and see matching
tasks. Search should not be case-sensitive.

When there are no matching tasks, the application should clearly
show that no results were found.

Clearing the search should restore the complete task list.
```

The first example describes **HOW**.

The second describes **WHAT and WHY**.

---

## Inspect `spec.md`

Open the generated specification.

Look for:

* User stories
* Functional requirements
* Acceptance scenarios
* Edge cases
* Success criteria
* Assumptions

### Specification Review

Answer:

**1. Who is the user?**

---

**2. What problem are they trying to solve?**

---

**3. List two acceptance criteria.**

1. ---

2. ---

**4. Find one requirement that could still be interpreted differently by two developers.**

---

That ambiguity is what we will address next.

---

# Part 7 — Clarify: Remove Ambiguity

**Suggested time: 20 minutes**

A specification can look detailed and still contain hidden assumptions.

Run:

```text
/speckit.clarify
```

Let the agent identify underspecified areas.

Do not answer automatically.

For every question, consider:

> Would two different developers make different implementation decisions because this requirement is unclear?

Example clarification questions might concern:

* What happens when no results exist?
* Is search case-sensitive?
* Who has permission?
* What happens when the network fails?
* How much data should be shown?
* Should deleted records appear?
* What does "fast" actually mean?

Respond to the clarification questions.

Then inspect the updated specification.

### Record Three Clarifications

| Before Clarification | Decision |
| -------------------- | -------- |
|                      |          |
|                      |          |
|                      |          |

### Reflection

Which clarification would have caused the biggest implementation problem if it had not been resolved?

---

Why?

---

---

# Part 8 — Plan: Decide HOW

**Suggested time: 25 minutes**

Only now should you start discussing implementation technology.

Run:

```text
/speckit.plan
```

Provide your project's technical constraints.

Example:

```text
/speckit.plan

Use the existing application architecture.

Frontend: React + TypeScript
Backend: Node.js + Express
Database: PostgreSQL

Reuse existing components and services where possible.

Do not introduce a new framework unless necessary.

Follow the project's current folder structure and testing approach.
```

Your prompt should match **your actual project**.

---

## Inspect the Technical Plan

Open:

```text
plan.md
```

Depending on the feature, Spec Kit may also produce supporting artifacts such as:

```text
research.md
data-model.md
contracts/
quickstart.md
```

Review them.

### Architecture Check

Answer:

**What files/components are expected to change?**

---

**Does the feature require database changes?**

---

**Does it introduce an API?**

---

**What testing strategy is proposed?**

---

---

# Part 9 — Tasks: Convert the Plan into Work

**Suggested time: 15 minutes**

A good technical plan still needs to become executable work.

Run:

```text
/speckit.tasks
```

Inspect:

```text
tasks.md
```

You should see concrete implementation activities rather than broad goals.

For example:

```text
Bad task:
Build search feature.

Better tasks:
- Add search input component.
- Add search state handling.
- Implement filtering logic.
- Handle empty search results.
- Add tests for case-insensitive matching.
- Add tests for empty results.
```

### Task Review

Find:

**One setup task**

---

**One implementation task**

---

**One testing task**

---

**One task that depends on another task**

---

---

# Part 10 — Analyze Before Coding

**Suggested time: 15 minutes**

We still have **not written the feature code**.

Before implementation, run:

```text
/speckit.analyze
```

The goal is to identify inconsistencies across the artifacts.

Think of the relationship as:

```text
Requirement
    ↓
Design Decision
    ↓
Implementation Task
```

Every important requirement should eventually have work that implements or validates it.

### Look for Problems

Examples:

```text
spec.md
"Search must be case-insensitive."

             ↓

plan.md
No explanation of search behavior.

             ↓

tasks.md
No test for case-insensitive search.
```

This represents a traceability problem.

### Record the Analysis

Did `/speckit.analyze` identify any issues?

**Yes / No**

If yes, record one:

---

How did you resolve it?

---

If necessary, update your specification, plan, or tasks before moving forward.

---

# Quality Gate

Do **not** execute `/speckit.implement` yet.

Ask yourself:

* Does the specification describe the feature clearly?
* Have important ambiguities been resolved?
* Does the plan satisfy the specification?
* Does the task list implement the plan?
* Are important acceptance criteria testable?
* Did the analysis identify unresolved inconsistencies?

Only proceed when the artifacts agree.

---

# Part 11 — Implement

**Suggested time: 30–40 minutes**

Now you are finally ready to code.

Run:

```text
/speckit.implement
```

Observe what the AI agent does.

Do not treat the process as magic.

Watch:

* Which task is being implemented?
* Which files are modified?
* Whether tests are created.
* Whether the constitution is followed.
* Whether tasks are completed in the expected order.
* Whether the implementation matches the specification.

---

# Part 12 — Validate the Result

Do not assume that generated code is correct simply because `/speckit.implement` completed.

Run your application.

Run the relevant tests.

Example:

```bash
npm test
```

or:

```bash
npm run test
```

Use whatever command is appropriate for your project.

Then manually validate the feature against its acceptance criteria.

---

# Acceptance Test

Return to:

```text
spec.md
```

For each important requirement, check the actual application.

| Requirement / Acceptance Criterion | Pass | Fail | Evidence |
| ---------------------------------- | ---- | ---- | -------- |
|                                    |      |      |          |
|                                    |      |      |          |
|                                    |      |      |          |
|                                    |      |      |          |

If something fails, determine whether the problem came from:

```text
Specification?
     ↓
Planning?
     ↓
Task generation?
     ↓
Implementation?
```

---

# Part 13 — Inspect the Complete SDD Chain

You should now have a chain similar to:

```text
Constitution
     ↓
Feature Specification
     ↓
Clarifications
     ↓
Technical Plan
     ↓
Implementation Tasks
     ↓
Consistency Analysis
     ↓
Code
     ↓
Tests
```

Pick **one requirement** from your project and trace it through the development artifacts.

### Requirement

---

### Where is it represented in the plan?

---

### Which task implements it?

---

### Which code implements it?

---

### How is it tested or verified?

---

This is called **traceability**.

---

# Part 14 — Git Review

Run:

```bash
git status
```

Then:

```bash
git diff
```

Inspect both the implementation and specification artifacts.

Do not commit automatically generated work without reviewing it.

If everything is correct:

```bash
git add .
git commit -m "Implement feature using Spec-Driven Development"
```

---

# Optional Challenge — `/speckit.checklist`

Spec Kit provides an additional quality-gate command:

```text
/speckit.checklist
```

Use it to generate a quality checklist for your feature.

For example, consider:

* Requirements completeness
* Security
* UX
* Accessibility
* API consistency
* Testing expectations

Compare the checklist with your current specification.

Did it reveal something that your original specification missed?

---

---

# Optional Challenge — `/speckit.converge`

After implementation, you can also explore:

```text
/speckit.converge
```

Use it to assess whether the implementation and SDD artifacts have converged or whether remaining work still exists.

Record anything it discovers:

---

---

# Final Reflection

Answer each question briefly.

## 1. Before this lab, how would you normally implement this feature?

---

---

## 2. What changed when you were forced to specify the feature before coding?

---

---

## 3. Which command had the greatest effect on implementation quality?

```text
/speckit.constitution
/speckit.specify
/speckit.clarify
/speckit.plan
/speckit.tasks
/speckit.analyze
/speckit.implement
```

Why?

---

---

## 4. What ambiguity did `/speckit.clarify` expose that you might otherwise have ignored?

---

---

## 5. Did `/speckit.analyze` identify anything that humans or the AI had missed?

---

---

## 6. What is one disadvantage or cost of adopting SDD?

---

---

## 7. For what kind of development work do you think SDD would be most useful?

---

---

---

# Lab Deliverables

Submit your repository containing:

1. Project constitution
2. `spec.md`
3. `plan.md`
4. `tasks.md`
5. Any generated supporting artifacts
6. Implemented source code
7. Relevant tests
8. Completed acceptance-test table
9. Final reflection

Your Git history should show the completed feature.

---

# Assessment Rubric

| Criteria                                                               |   Weight |
| ---------------------------------------------------------------------- | -------: |
| Project constitution establishes meaningful engineering principles     |      10% |
| Specification clearly defines user needs and acceptance criteria       |      20% |
| Ambiguities are identified and resolved                                |      10% |
| Technical plan appropriately translates requirements into architecture |      15% |
| Tasks are clear, actionable, and traceable to the plan                 |      10% |
| Analysis is performed and inconsistencies are addressed                |      10% |
| Implementation satisfies the specification                             |      15% |
| Validation, testing, and reflection                                    |      10% |
| **Total**                                                              | **100%** |

---

# Key Takeaway

The purpose of this lab is **not to learn seven slash commands**.

The commands represent different software-engineering decisions:

```text
Constitution → What principles must we follow?

Specify      → What are we building and why?

Clarify      → What don't we understand yet?

Plan         → How should we build it?

Tasks        → What exactly needs to be done?

Analyze      → Do our requirements, design and tasks agree?

Implement    → Now, write the code.

Validate     → Did we actually build what we specified?
```

The central idea of Spec-Driven Development is:

> **The specification drives the implementation; the implementation does not define the specification.**
